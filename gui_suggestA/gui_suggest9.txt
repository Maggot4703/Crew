9. Layout Resilience
   - Add guards to verify widgets exist before grid/pack calls, and wrap dynamic layout changes in safe-checks to prevent crashes.
   - Use consistent `grid` weight and sticky configurations to ensure responsive resizing.

---

# Detailed Implementation Guidance (Appended)

- Add guards to verify widgets exist before calling `grid`/`pack`.
- Use consistent `grid` weight and sticky settings for responsive resizing.
- Add widget existence checks and update layout configuration.
- No changes to TTS logic or setup.

