6. Centralized Configuration Management
   - Store UI settings (window size/position, scripts_dir) in `config.py` or a JSON file and load/save on startup/exit.
   - Validate restored geometry against current screen bounds to avoid off-screen windows.

---

# Detailed Implementation Guidance (Appended)

- Store UI settings (window size, position, scripts_dir) in `config.py` or a JSON file.
- Load on startup, save on exit.
- Validate geometry to avoid off-screen windows.
- Add config load/save logic and update window geometry handling.
- No changes to TTS logic or setup.

