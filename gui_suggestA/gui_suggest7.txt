7. Enhanced Logging
   - Replace basic file handler with `logging.handlers.RotatingFileHandler` to automatically rotate logs and limit file size.
   - Include contextual information (timestamp, thread name, function name) in log format.

---

# Detailed Implementation Guidance (Appended)

- Use `logging.handlers.RotatingFileHandler` to rotate logs and limit file size.
- Add contextual info (timestamp, thread name, function name) to log format.
- Update logging setup in `config.py` or main script.
- No changes to TTS logic or setup.

