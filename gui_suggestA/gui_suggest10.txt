10. Unit and Integration Testing
    - Write unit tests for non-UI logic (e.g., text chunking, filter application, configuration load/save).
    - Explore GUI testing tools like `pytest-tkinter` or `tkintertest` for automated integration tests of key flows.

---

# Detailed Implementation Guidance (Appended)

- Write unit tests for non-UI logic (e.g., text chunking, config load/save).
- Use tools like `pytest-tkinter` or `tkintertest` for GUI integration tests.
- Add or expand test files as needed.
- No changes to TTS logic or setup.

